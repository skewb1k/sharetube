{
  "name": "sharetube-extension",
  "type": "module",
  "scripts": {
    "build": "tsc && node build.js",
    "lint": "prettier --check .",
    "fmt": "prettier --write ."
  },
  "devDependencies": {
    "chrome-types": "^0.1",
    "prettier": "^3",
    "typescript": "^5"
  }
}
